{
  "name": "ixmaps-grammar",
  "version": "0.1.8",
  "description": "Machine-readable ixmaps theme grammar + API registry, and a static checker for ixmaps / ixmaps-gl pages",
  "type": "module",
  "bin": {
    "ixmaps-check": "bin/ixmaps-check.mjs"
  },
  "main": "src/check.mjs",
  "scripts": {
    "extract": "node scripts/extract-grammar.mjs",
    "build": "node scripts/build-runtime.mjs",
    "test": "node --test 'test/*.test.mjs'"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "BSD-3-Clause",
  "dependencies": {
    "acorn": "^8.18.0",
    "ajv": "^8.20.0"
  }
}
